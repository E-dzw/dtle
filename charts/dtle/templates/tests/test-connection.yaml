apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dtle.fullname" . }}-test-connection"
  labels:
    {{- include "dtle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dtle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
